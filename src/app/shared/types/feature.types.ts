export interface Feature {
  title: string;
  description: string;
  link: string;
  linkText: string;
  icon: string;
}

export interface Advantage {
  title: string;
  description: string;
  icon: string;
}